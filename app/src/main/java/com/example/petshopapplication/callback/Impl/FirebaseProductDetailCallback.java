package com.example.petshopapplication.callback.Impl;

import com.example.petshopapplication.callback.FirebaseCallback;
import com.example.petshopapplication.model.ProductDetail;

public class FirebaseProductDetailCallback implements FirebaseCallback<ProductDetail> {
    @Override
    public void onCallback(ProductDetail object) {
        if(object != null) {

        }
    }
}
